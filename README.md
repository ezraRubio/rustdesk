Fork of rustdesk. Used for compiling librustdesk.so it's available for Fort-RemoteControl.


Considering you have the prerequisites:

android.sh can build the whole apk (including flutter)

native.sh builds only librustdesk.so (including flutter rust bridge)
